import { Weekday } from './weekday';
import { MonthWeek } from './month-week';
import { Month } from './month';

import { Injectable } from '@angular/core';

@Injectable ()
export class MonthService {

	/*getMonth(year: number = -1, month: number = -1): string {
		new Month();
	}*/
}